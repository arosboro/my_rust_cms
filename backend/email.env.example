# Email Configuration for Rust CMS Backend
# Copy this file to .env and update with your settings

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/my_rust_cms

# Server Configuration
BACKEND_PORT=8081
BACKEND_HOST=127.0.0.1
RUST_ENV=development
RUST_LOG=info

# Email Configuration
# Set to "true" to enable real email sending (uses SMTP settings below)
# Set to "false" to use mock email service (prints to console only)
USE_REAL_EMAIL=true

# SMTP Settings (only used when USE_REAL_EMAIL=true)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=CMS System
BASE_URL=http://localhost:3000

# Instructions:
# 1. Copy this file: cp email.env.example .env
# 2. Update the SMTP settings with your actual values
# 3. For Gmail: Use app-specific password, not your regular password
# 4. Restart the backend: cargo run
